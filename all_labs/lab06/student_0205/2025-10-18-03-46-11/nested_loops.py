# Author   : REDACTED
# Email    : REDACTED
# Spire ID : REDACTED



first_names = ['Ari' , 'Taylor']
last_names = ['Levine', 'Lopez' , 'Khan' , 'Wang']
full_names = []

#def get_names(first_names, last_names): 
 #   for name in first_names:
  #      full_names = [first_names + '_' + last_names] 

   #     full_names.append(first_names + last_names)

    #return(full_names)

#get_names(first_names, last_names)



scores = [[(90, 0), (80, 1), (70, 2), (60, 3), (50, 4)],
           [(100, 10), (90, 0), (80, 0), (90, 0)], 
           [(0, 0), (20, 0), (40, 1), (100, 0), (100, 0), (100, 0)]]
average_scores = []

def average_scores(score):
    for score in scores(0):
        grade = (90 + 80 * 0.9 + 70 * 0.75 + 60 * 0.5 + 50 * 0) / 5 
        average_scores.append(grade) 


    #for score in scores(1):
     #   grade 
    #for score in scores(2): 
      #  grade 

