# Author: REDACTED
# Email: REDACTED
# SPIRE ID: REDACTED


def get_names(first_names, last_names):
    full_names = []  # create an empty list
    for first in first_names:
        for last in last_names:
            full_name = first + " " + last  # add space between names
            full_names.append(full_name)
    return full_names
