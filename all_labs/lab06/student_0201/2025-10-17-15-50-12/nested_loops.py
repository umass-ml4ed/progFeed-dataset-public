# Author: REDACTED
# Email: REDACTED
# Spire ID: REDACTED

def get_names(first_names,last_names):
    full_names = []
    for first_names in []:
        for last_names in []:
            full_names.append(f'{first_names} ' + '{last_names}', end=' ')
    return full_names

    
def average_scores(grade,lateness):
    scores = []
    for lateness in range():
        for grade in range (0,100):
            scores.append(f'{(grade*lateness)/len(grade)}')
    return scores